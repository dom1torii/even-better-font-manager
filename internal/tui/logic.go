package tui

import (
	// "github.com/dom1torii/even-better-font-manager/internal/config"
)

// func (m *model) addFontToCollection(f config.Font) {
//   // dont add if font already exists
//   for _, existing := range m.fonts.collection.Fonts {
//     if existing.Path == f.Path {
//       return
//     }
//   }

//   m.fonts.collection.Fonts = append(m.fonts.collection.Fonts, f)
//   m.fonts.collection.Save()
// }
